package ru.gb.SOLID.OCP.Entities;

public class Bus extends Vehicle {

    public Bus(double maxSpeed, double allowedSpeedLimiter, String type) {
        super(maxSpeed, allowedSpeedLimiter, type);
    }
}
